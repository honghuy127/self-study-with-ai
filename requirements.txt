# Runtime dependency of the tools. The vendored dossier scripts under
# tools/research/ are standard-library only; everything else needs YAML.
PyYAML>=6.0

# Developing the tools themselves also wants:
#   pip install -r requirements-dev.txt
